请直接修改当前工程中的 image.c、image.h 和 config.h，解决十字区域补线断裂、竖直、折线突变，无法直接连接上下真实边线的问题。

一、当前问题

当前十字补线的核心问题不是线性插值公式错误，而是补线区间和补线端点判断错误。

现有流程存在以下问题：

1. search_edges() 每行从种子点向左右寻找最近的黑白跳变。在十字区域存在内部黑块、横向分支和多个白色连通区域时，算法会把十字内部黑块边缘误认为真实赛道边线。
2. cross_fill_gap() 只处理连续满足 left_lost[r] && right_lost[r] 的行。

但是十字区域中的错误内部边缘经常会被识别为“实测边”，对应的 lost 标志为 0。这会把原本完整的十字缺口切割成多段，使其无法进入真正的十字插值逻辑。

1. 没有进入 cross_fill_gap() 的行会进入普通宽度重建。双边丢失时沿用 last_mid，因此会生成竖线；单边丢失时通过固定宽度平移，容易产生断线和折线。
2. 当前单边有效时直接使用 seed = left 或 seed = right 作为下一行种子。这样会使种子贴在边界上，进入十字后容易跟踪到内部黑块或横向分支。
3. 当前宽度表允许十字区域的异常宽度和错误边缘参与更新，导致后续单边重建继续偏移。

最终目标：

在确定十字上下稳定边线后，无论中间区域原来是双边丢失、单边丢失，还是存在错误的“实测边”，都必须废弃中间区域的原始结果，将下方真实边线和上方真实边线直接连接起来。

二、修改要求

1. 修正逐行种子传播

修改 search_edges() 的种子更新逻辑。

增加局部变量：

last_good_width
previous_mid

初始化方式：

last_good_width = WIDTH_TABLE_DEFAULT
previous_mid = ti->longest_col

每行搜索完成后，按照以下规则更新种子。

左右边都有效时：

计算本行真实中点。

如果本行宽度处于合理范围，并且与上一可信宽度差值不大，则更新 last_good_width。

使用本行中点作为下一行种子。

更新 previous_mid。

只有左边有效时：

不允许直接使用 seed = ti->left[r]。

使用下面的方式估算道路内部中点：

seed = ti->left[r] + last_good_width / 2

计算结果必须限制在 0 到 IMG_W - 1 范围内。

只有右边有效时：

不允许直接使用 seed = ti->right[r]。

使用下面的方式估算道路内部中点：

seed = ti->right[r] - last_good_width / 2

计算结果必须限制在 0 到 IMG_W - 1 范围内。

左右边都丢失时：

继续使用 previous_mid。

不允许把种子移动到图像左右边缘。

只有在左右边均有效、宽度可信、边线连续时，才更新 previous_mid 和 last_good_width。

1. 增加逐行十字补线标记

在 track_info_t 中增加以下字段：

uint8_t cross_filled[IMG_H];
uint8_t cross_valid;
uint8_t cross_lo;
uint8_t cross_hi;

字段含义：

cross_filled[r] 表示该行是否被十字连接结果覆盖。

cross_valid 表示本帧是否找到了可靠的十字连接区间。

cross_lo 表示靠近车辆一侧的补线起始行。

cross_hi 表示远离车辆一侧的补线结束行。

必须统一 cross_hi 的语义。建议采用左闭右开区间，即补线范围为 cross_lo 到 cross_hi - 1。

每帧开始处理时，必须完整初始化这些字段，禁止遗留上一帧的数据。

初始化建议：

cross_valid = 0
cross_lo = 0
cross_hi = 0
cross_filled 所有元素清零

1. 不再只用双边丢失判断十字缺口

增加一个十字可疑行判断函数，例如：

static uint8_t is_cross_suspect_row(...)

十字可疑行至少包括以下情况：

左右边同时丢失。

只有一侧边线存在。

当前实测宽度相对于可信宽度突然明显增大。

当前实测宽度相对于可信宽度突然明显减小。

左边线相对上一稳定行发生过大跳变。

右边线相对上一稳定行发生过大跳变。

当前中点相对上一稳定中点发生过大跳变。

当前实测边位于正常赛道内部，明显可能是十字内部黑块的边缘。

不能要求十字区域内每一行都连续可疑。

错误实测边可能使中间一两行暂时不满足可疑条件，因此十字区间搜索必须允许少量空洞。

在 config.h 中增加：

\#define CROSS_SUSPECT_HOLE_MAX 2

含义：

十字可疑区间内部允许出现的最大连续非可疑行数。

如果可疑区间中只出现一两行错误实测边，不允许因此把整个区间切成两段。

1. 增加稳定锚点判断

不能继续直接使用 gap_lo - 1 和 gap_hi 作为唯一补线端点。

增加稳定行判断函数，例如：

static uint8_t is_stable_anchor_row(...)

稳定行必须满足以下条件：

左右边均为真实测量。

left_lost[r] 为 0。

right_lost[r] 为 0。

当前宽度处于合理范围。

当前宽度与附近可信宽度差值不超过指定阈值。

左边线与相邻行连续。

右边线与相邻行连续。

中点与相邻行连续。

当前行不是十字可疑行。

当前行不是已经补线或普通预测产生的行。

十字候选区间必须同时具有：

下方连续若干稳定行。

上方连续若干稳定行。

在 config.h 中增加以下参数：

\#define CROSS_ANCHOR_ROWS 4

表示上下锚点窗口至少需要的连续稳定行数。

\#define CROSS_EDGE_JUMP_MAX 12

表示相邻行单侧边线允许的最大跳变像素。

\#define CROSS_MID_JUMP_MAX 10

表示相邻行中点允许的最大跳变像素。

\#define CROSS_WIDTH_DELTA_MAX 24

表示十字判断中，实测宽度与可信宽度允许的最大差值。

\#define CROSS_SUSPECT_MIN_ROWS 8

表示十字可疑区间的最小长度。

\#define CROSS_SUSPECT_HOLE_MAX 2

表示十字可疑区间内部允许出现的最大非可疑空洞长度。

\#define WIDTH_UPDATE_DELTA_MAX 16

表示更新可信宽度时，当前宽度与上一可信宽度允许的最大差值。

所有新增参数必须放在 config.h 中，不允许在 image.c 函数内部直接写魔法数字。

每个参数必须添加中文注释，并说明单位是像素或行数。

1. 使用稳定窗口确定上下连接端点

对左边线和右边线分别处理。

在十字候选区间下方选取 CROSS_ANCHOR_ROWS 个连续稳定行。

在十字候选区间上方选取 CROSS_ANCHOR_ROWS 个连续稳定行。

不能只信任单独一行。

建议对稳定窗口使用整数最小二乘直线拟合。

拟合形式为：

edge = a × row + b

分别拟合以下四组数据：

下方左边线。

上方左边线。

下方右边线。

上方右边线。

然后根据拟合结果，在十字区间边界位置计算四个连接端点：

左边下端点。

左边上端点。

右边下端点。

右边上端点。

可以增加辅助函数，例如：

static uint8_t fit_edge_line_q8(...)

或者：

static uint8_t estimate_anchor_edge(...)

实现要求：

使用定长数组或直接累加计算。

不使用动态内存。

不使用递归。

不使用浮点运算。

斜率可以使用 Q8 或 Q10 定点数表示。

中间乘加使用 int32_t。

防止除零。

防止整数溢出。

保持适合 TC264 实时运行。

如果不使用最小二乘，也可以使用中值和局部趋势组合，但不能直接取单独一行。

1. 覆盖整个十字区间，而不是只覆盖 lost 行

重写现有十字补线函数。

可以将 cross_fill_gap() 修改为：

static uint8_t cross_fill_span(track_info_t *ti, uint8_t start, uint8_t end)

其中 start 为补线起始行，end 为左闭右开区间的结束行。

函数必须覆盖 start 到 end - 1 之间的所有行，包括：

双边丢失行。

左边丢失行。

右边丢失行。

被错误识别为实测边的行。

不允许因为某一行的 lost 标志为 0，就保留该行原来的错误边线。

左边线使用下方稳定左端点到上方稳定左端点进行线性连接。

右边线使用下方稳定右端点到上方稳定右端点进行线性连接。

每一行必须重新写入：

ti->left[r]
ti->right[r]
ti->mid[r]
ti->cross_filled[r] = 1

插值建议使用带四舍五入的整数运算，避免整除持续向同一方向截断。

插值结果必须满足：

left 小于 right。

left 大于等于 0。

right 小于 IMG_W。

中线必须通过左右边平均计算，不能单独插值中线。

如果出现以下情况，必须放弃该候选区间，不得强行补线：

上下端点无效。

左右端点交叉。

补线后某一行 left 大于等于 right。

补线宽度小于 WIDTH_MIN_PX。

补线宽度大于 WIDTH_MAX_PX。

补线宽度相邻行变化异常。

上下边线对应关系明显错误。

补线完成后设置：

ti->cross_valid = 1
ti->cross_lo = start
ti->cross_hi = end
ti->inflect_row = start

1. 普通重建不能覆盖十字补线结果

在 rebuild_lost_edges() 后续的逐行普通重建中，首先检查：

if (ti->cross_filled[r])

当 cross_filled[r] 为 1 时：

保留十字补线已经写入的 left、right 和 mid。

禁止再次执行普通双边丢失重建。

禁止再次执行左边丢失重建。

禁止再次执行右边丢失重建。

允许更新 last_mid，但必须使用十字补线后的 mid。

只有 cross_filled[r] 为 0 时，才能进入原来的普通重建流程。

1. 保护宽度表

当前宽度表不能只判断宽度是否处于 WIDTH_MIN_PX 到 WIDTH_MAX_PX 范围内。

只有同时满足以下条件时，才允许更新可信宽度：

左右边都没有丢失。

当前宽度处于 WIDTH_MIN_PX 到 WIDTH_MAX_PX 范围内。

当前宽度与上一可信宽度差值不超过 WIDTH_UPDATE_DELTA_MAX。

当前行不是十字可疑行。

当前行不在十字候选区间内。

当前行不在十字补线区间内。

当前左边线与附近稳定行连续。

当前右边线与附近稳定行连续。

当前中点没有明显跳变。

如果当前宽度不可信，则继续沿用上一可信宽度。

十字候选区域和十字补线区域内，禁止使用异常宽度更新宽度表。

不能通过简单扩大 WIDTH_MIN_PX 或 WIDTH_MAX_PX 解决问题。

1. 修改十字检测逻辑

当前 detect_cross() 不能继续只检查连续的 left_lost && right_lost 区间。

新的十字区间搜索已经综合判断了：

可疑区间长度。

内部允许空洞。

上下稳定锚点。

边线跳变。

宽度异常。

端点有效性。

因此 detect_cross() 应优先使用 ti->cross_valid。

建议修改为：

如果 ti->cross_valid 为 1，则返回 1。

如果 ti->cross_valid 为 0，则返回 0。

也可以在 detect_cross() 内再次检查 cross_lo、cross_hi 和区间长度，但不能再次使用旧的严格连续双边丢失条件否定已经确认的十字区间。

必须保留 detect_cross() 的函数名称和外部接口，避免影响其他文件调用。

1. 修正转向权重

当前 weighted_error() 在 det_cross 为真时，会给所有双边丢失行统一使用十字补线权重。

这会错误影响不属于十字补线区间的普通双边丢失行。

修改 weighted_error() 的逐行权重判断顺序。

优先判断：

if (ti->cross_filled[r])
{
w = w * STEER_W_CROSS_FILL_PCT / 100;
}
else if (ti->left_lost[r] && ti->right_lost[r])
{
w = w * STEER_W_BOTH_LOST_PCT / 100;
}
else if (ti->left_lost[r] || ti->right_lost[r])
{
w = w * STEER_W_SINGLE_EDGE_PCT / 100;
}

十字补线权重只能作用于 cross_filled[r] 为 1 的行。

不能再通过 ti->det_cross 为真，就把所有双边丢失行都当作十字补线行。

1. 保留原始丢线信息

十字补线时，不要把 left_lost[] 和 right_lost[] 全部清零。

left_lost[] 和 right_lost[] 表示原始边线搜索结果。

环岛检测、图像保护、统计信息和状态机可能仍然需要原始丢线信息。

十字补线结果必须通过 cross_filled[] 单独表示。

both_lost_rows 应按照原始双边丢失状态统计。

不能因为补线后已经生成坐标，就把原始双边丢失信息抹掉。

普通图像失效保护应继续基于原始可见性进行判断，避免补线数据掩盖摄像头真实丢线。

1. 调整处理流程顺序

将 image_process() 和 rebuild_lost_edges() 的职责整理清楚。

建议处理顺序为：

第一步，二值化。

第二步，计算最长白列。

第三步，执行原始左右边搜索。

第四步，建立初始可信宽度和边线连续性信息。

第五步，逐行生成十字可疑标记。

第六步，合并带少量空洞的十字可疑区间。

第七步，筛选长度满足要求的十字候选区间。

第八步，为候选区间寻找下方稳定锚点窗口。

第九步，为候选区间寻找上方稳定锚点窗口。

第十步，拟合上下左右边线并计算连接端点。

第十一步，覆盖整个十字区间。

第十二步，对没有被 cross_filled[] 覆盖的普通丢线行进行宽度重建。

第十三步，执行十字、环岛和坡道检测。

第十四步，计算曲率。

第十五步，计算加权误差。

不能修改当前图像行坐标定义。

row = 0 仍然表示靠近车辆的位置。

行号增大仍然表示逐渐远离车辆。

1. 十字候选区间搜索要求

在 CROSS_BAND_ROW_LO 到 CROSS_BAND_ROW_HI 范围内搜索十字可疑区间。

搜索时需要维护：

候选起点。

当前结束位置。

连续非可疑行数量。

候选区间内的可疑行数量。

当连续非可疑行数量不超过 CROSS_SUSPECT_HOLE_MAX 时，继续保留当前候选区间。

当连续非可疑行数量超过 CROSS_SUSPECT_HOLE_MAX 时，结束当前候选区间。

候选区间的真实结束位置需要去除末尾多余的非可疑行。

候选区间至少满足：

总跨度不小于 CROSS_SUSPECT_MIN_ROWS。

有效可疑行数量达到合理比例。

上下方都能找到稳定锚点窗口。

如果同一帧存在多个候选区间，优先选择：

上下锚点最稳定的区间。

有效可疑行最多的区间。

区间跨度合理的区间。

距离车辆较近且符合十字特征的区间。

不能简单选择遇到的第一个区间。

1. 防止普通弯道被误判为十字

普通弯道中左右边线可能持续倾斜，但通常具有连续性。

十字可疑判断不能只根据边线斜率较大或中点移动判断。

必须区分“连续弯曲”和“突然跳变”。

普通弯道中：

左边线逐行变化应相对连续。

右边线逐行变化应相对连续。

道路宽度变化应相对平滑。

中点变化方向应相对一致。

十字区域中通常会出现：

一侧或两侧突然丢失。

宽度突然扩张或收缩。

边线跳到内部黑块。

中点突然跳变。

搜索到的白色区域不再符合上一行道路走廊。

is_cross_suspect_row() 必须综合多个条件。

不允许仅凭某一个条件直接判定整行为十字可疑行。

建议使用计分方式。

例如满足以下任意多个条件后，才判为可疑：

双边丢失，直接判为高置信可疑。

单边丢失，增加可疑分数。

宽度异常，增加可疑分数。

左边跳变，增加可疑分数。

右边跳变，增加可疑分数。

中点跳变，增加可疑分数。

只有综合分数达到阈值时，才标记为十字可疑行。

所有计分阈值必须定义在 config.h 中。

1. 防止补错对应边

补线时必须保证左边连接左边，右边连接右边。

不能简单按照数值远近交换左右边。

上下锚点必须满足：

下方左端点小于下方右端点。

上方左端点小于上方右端点。

插值后的每一行左边都小于右边。

如果出现边线交叉，说明端点选择错误，必须放弃该候选区间。

不要继续使用原 cross_fill_gap() 中遇到 left 和 right 反转时直接交换两者的做法。

十字补线过程中如果 left 大于 right，不能交换，因为交换会掩盖锚点匹配错误。

正确做法是判定本次补线失败，并回退到普通重建。

1. 增加补线连续性验证

cross_fill_span() 完成计算后，不要立即写入最终结构。

建议先在小型定长临时数组中计算，或者先逐行验证后再写入。

需要检查：

每一行 left 小于 right。

每一行宽度合法。

相邻行左边变化不超过合理限制。

相邻行右边变化不超过合理限制。

相邻行中点变化不超过合理限制。

补线起始处与下方实测边连续。

补线结束处与上方实测边连续。

如果任意验证失败，整个候选区间补线失败。

失败后不得只保留部分补线结果。

不允许出现补了一半又回退普通重建的混合状态。

1. 上位机显示兼容

保持 left、right、mid、left_lost 和 right_lost 的现有含义，避免破坏上位机解析。

新增的 cross_filled[]、cross_valid、cross_lo 和 cross_hi 用于明确区分十字补线。

如果上位机当前根据 lost 标志显示预测线，可以进行必要兼容修改：

cross_filled[r] 为 1 时，显示为十字补线颜色。

普通单边或双边重建继续使用原预测线颜色。

真实测量边继续使用原实测线颜色。

但不能只修改显示效果。

核心算法修正必须在 image.c 中完成。

即使不上位机，嵌入式端输出的 left、right 和 mid 也必须正确。

1. 代码质量要求

所有新增函数必须使用 static 修饰，除非需要对外公开。

所有新增函数必须写清楚中文注释。

所有区间必须明确是闭区间还是左闭右开区间。

建议统一使用左闭右开区间，避免 end 行是否参与补线产生混乱。

所有行号运算需要防止 uint8_t 下溢。

涉及 start - 1、row - 1 等操作时，必须先判断是否大于 0，或者使用 int16_t 临时变量。

涉及 IMG_W - 1、valid_rows - 1 时，必须确保对应值不为 0。

禁止数组越界。

禁止未初始化变量。

禁止修改后留下编译警告。

三、必须保持的约束

1. 不修改以下公开函数名称：

image_process()
image_track_invalid()
detect_cross()
detect_ring_left()
detect_ring_right()
detect_ramp()

1. 不修改图像尺寸定义。
2. 不修改原始坐标方向。
3. 不引入动态内存。
4. 不使用递归。
5. 不使用大型局部数组。
6. 不依赖上位机完成算法修正。
7. 不通过简单扩大 CROSS_MIN_BOTH_LOST 规避问题。
8. 不通过修改显示颜色或隐藏错误线条规避问题。
9. 不允许只连接中线。

必须分别连接左边线和右边线，再根据左右边计算中线。

1. 不允许把十字区域简单补成两条固定竖线。
2. 保留现有环岛、坡道、失效保护和最长白列流程。
3. 除了兼容新增字段和避免相互冲突，不要大范围重写无关逻辑。
4. 所有新增阈值必须集中定义在 config.h 中。
5. 修改后的代码必须完整可编译。
6. 不能保留伪代码、TODO、未实现函数或仅给出修改建议。
7. 需要直接完成代码修改，而不是只分析问题。

四、修改后必须达到的行为

当十字区域出现内部黑块或横向分支时：

内部黑块边缘不能继续作为最终赛道边线。

少量错误实测行不能把十字补线区间切成多段。

下方绿色斜边必须直接连接到上方对应的绿色真实边。

下方红色斜边必须直接连接到上方对应的红色真实边。

左右两侧连接应为连续直线或平滑的近似直线。

中间不能再出现长竖线。

中间不能再出现悬空线。

中间不能再出现断线。

中间不能再出现突然横向折转。

中间错误识别到的实测边必须被十字补线覆盖。

十字补线区域内的中线必须由补线后的左右边平均得到。

普通弯道不能因为边线持续倾斜就被误判为十字。

没有可靠上下锚点时，不得强行补线。

没有可靠锚点时，应完整回退到现有普通重建逻辑。

补线失败时，不能留下部分写入的数据。

五、完成修改后需要说明的内容

完成代码修改后，请使用纯文本说明以下内容：

1. 修改了哪些文件和函数。
2. 在 config.h 中新增了哪些参数，每个参数的作用是什么。
3. 十字可疑行是如何判断的。
4. 带少量空洞的十字候选区间是如何合并的。
5. 如何防止错误实测边把补线区间切断。
6. 下方稳定锚点如何确定。
7. 上方稳定锚点如何确定。
8. 左右边线端点如何通过稳定窗口计算。
9. 整个十字区间如何被统一覆盖。
10. 为什么普通宽度重建不会再次覆盖十字补线。
11. 为什么十字内部黑块边缘现在会被废弃。
12. 如何防止普通弯道误判成十字。
13. 如何防止左右连接线交叉。
14. 补线验证失败时如何回退。
15. 修改后的代码是否完成编译检查，以及是否存在编译警告。