# 智能车摄像头控制工程总结

## 项目概述

本项目是**智能车竞赛**的自动驾驶小车固件，基于**Infineon AURIX TC264D**双核MCU开发。小车通过摄像头采集赛道图像，提取赛道几何信息，通过有限状态机做出驾驶决策，最终控制舵机和电机完成自主行驶。

**当前精简目标**：测试赛道仅含**直道 + 转弯**。已移除十字补线/检测、环岛状态机，以及行数/曲率/boost 等速度调度逻辑；速度改为定速开环（`STRAIGHT_DUTY` + 斜坡）。

**核心硬件限制**：无编码器、无刹车，只能靠惯性滑行减速。

---

## 目录结构

```
test_CarRun/
├── config.h              # 主配置文件（所有可调参数的唯一来源）
├── pins.h                # MCU引脚定义（目前为占位符）
├── isr_config.h          # TC264中断优先级配置
├── cpu0_main.h / .c      # CPU0 入口和主循环
├── cpu1_main.c           # CPU1 空桩（预留）
├── image.h / .c          # 18th 最长白列 + 十字补线 + 坡道/环岛检测
├── fsm.h / .c            # 驾驶决策有限状态机（精简版）
├── control.h / .c        # 转向PD + 定速开环占空比
├── motor.h / .c          # 硬件抽象层：舵机、电机、蜂鸣器、按键
├── display.h / .c        # IPS200 LCD显示 + 无线遥测输出
├── menu.h / .c           # 数据驱动参数调试菜单引擎
├── menu_port.h / .c      # 菜单硬件移植层
├── menu_config.c         # 菜单页面/项目定义
├── perf.h / .c           # 管线性能剖析钩子
├── isr.h / .c            # 中断服务例程
└── docs/                 # 学习文档
```

**外部依赖**（不在仓库内）：
- `libraries/` — SeekFree TC264开源库（提供外设驱动）

---

## 硬件平台

| 组件 | 详情 |
|------|------|
| **MCU** | Infineon AURIX TC264D（双核TriCore, 200MHz） |
| **摄像头** | MT9V03X 灰度摄像头，188x120 像素，50 FPS，DMA 传输 |
| **显示屏** | 2.0" IPS200 LCD，320x240，SPI 接口 |
| **舵机** | 标准 RC 舵机，50 Hz PWM，中位 1500µs |
| **驱动电机** | 单路有刷直流电机，~17kHz PWM，**仅前进** |
| **编码器** | **无** |
| **无线** | SeekFree 无线UART模块，115200 bps |
| **按键** | 4个（解锁、页面切换、滑行测试、保留） |
| **蜂鸣器** | 有源蜂鸣器，用于状态切换音频提示 |
| **传感器** | IMU(ICM-20602/IMU660RC) + ToF测距（中断已接入，控制逻辑未使用） |
| **Flash** | 片上 DFLASH，用于持久化菜单参数 |
| **电池** | 2S锂电池（7.4V-8.4V），无电压ADC补偿 |
| **工具链** | Infineon AURIX Development Studio (ADS) v1.10.2 + iLLD |
| **许可证** | GPL 3.0 |

---

## 软件架构（分层设计）

```
Layer 6: 主循环 (cpu0_main.c)
Layer 5: 显示/遥测 (display.c) + 菜单系统 (menu.c)
Layer 4: 控制 (control.c) — 转向PD + 定速开环
Layer 3: 状态机 (fsm.c) — 驾驶决策逻辑
Layer 2: 图像处理 (image.c) — 18th 最长白列 + 十字补线
Layer 1: 硬件抽象 (motor.c, isr.c) + 配置 (config.h, pins.h)
```

**数据流**：
```
摄像头 MT9V03X (DMA)
    ↓
image_process() → track_info_t（赛道信息）
    ↓
fsm_update() → state_contract_t（只读控制合约）
    ↓
control_update() → control_out_t（舵机/电机输出）
    ↓
motor_apply() → PWM硬件
```

**关键设计原则**：
- **纯逻辑/硬件分离**：`image.c`、`fsm.c`、`control.c` 不包含任何MCU头文件
- **配置集中化**：所有可调参数在 `config.h` 中统一定义
- **合约驱动架构**：状态机只生产 `state_contract_t`，控制模块读取并执行
- **Fail-safe优先**：`DEBUG_NO_DRIVE=1` 可强制零动力输出

---

## 图像处理管线

### 1. Otsu 自适应阈值
- 基于直方图的 Otsu 方法，自动寻找最佳全局阈值
- 备选：固定阈值模式（`USE_FIXED_THRESHOLD`）

### 2. 最长白列种子点 + 边缘搜索 / 混合跟踪
- 默认 `USE_HYBRID_TRACKING=1`：八邻域双边跟踪
- 关闭时：最长白列锚定 + 由内向外搜边 + 丢线宽度重建

### 3. 加权转向误差
- 图像分为8个水平带，低速/高速两套权重表按占空比混合

### 4. 曲率估计（Q8 定点数）
- `far_segment_slope - near_segment_slope`（遥测/显示用；当前速度策略不再查曲率表）

### 5. 元素检测器（精简后）

| 检测器 | 状态 |
|--------|------|
| **十字弯** | **已删除** |
| **环岛（左/右）** | **已删除** |
| **坡道** | 占位实现，默认 `ENABLE_RAMP=0` |

---

## 有限状态机

状态枚举：`NORMAL` → `RAMP` → `RECOVERY` → `FAULT`

**已删除**：`CROSS`、`RING_PRE`、`RING_IN`、`RING_EXIT`

**核心机制**（保留）：
- **M/N消抖**：坡道检测器（默认关闭）
- **元素超时** → `RECOVERY`，恢复失败 → `FAULT`
- **退出冷却**：脱离元素后抑制重检测

---

## 控制系统

### 转向（PD控制器）
- **比例增益调度**：`Kp(e)` 随误差二次插值
- **微分项**：EMA 滤波；状态切换首帧归零
- **舵机限幅**：每帧最大变化 `SERVO_SLEW_LIMIT`

### 速度（定速开环，已去掉调度）
```
目标占空比 = min(STRAIGHT_DUTY, contract_duty_cap, DUTY_HARD_CAP)
```
- **已删除**：行数减速表、曲率减速表、转向减速、boost、出弯再加速确认、环岛速度覆盖
- 仍保留升/降斜坡（软启动 / 瞬间切油滑行）

---

## 调试与显示

- **IPS200 LCD**：概述 / 图像叠加 / 状态机轨迹
- **无线遥测**：UART 输出运行状态
- **菜单系统**：运行时调参并 Flash 持久化
- **蜂鸣器**：坡道/恢复/故障提示音

---

## 编程语言与编译

- **语言**：C（C99标准）
- **工具链**：Infineon AURIX Development Studio (ADS) v1.10.2
- **框架**：SeekFree TC264 开源库 + Infineon iLLD

---

## 设计亮点总结

1. **合约驱动架构**使状态机与控制逻辑解耦
2. **纯逻辑模块**便于理解与离线对照
3. **配置集中化**避免魔数散布
4. **Fail-safe优先**，硬件看门狗与逻辑检查双重保障
5. **精简赛道元素**：直道+转弯测试路径下去掉十字/环岛与复杂速度调度
