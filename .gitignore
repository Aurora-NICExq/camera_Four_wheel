/target/
*.o
*.so
*.dylib
*.d
.DS_Store

# Agent skills (local tooling, not versioned)
.agents/
skills-lock.json

# Host-side tools (Mac sim / replay, not versioned)
cross_sim/
run_cross_sim.command

# Markdown docs (local notes, not versioned)
*.md
